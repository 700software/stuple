{
  "name": "stuple",
  "version": "0.2.0",
  "description": "More to come! :)",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "publish": "npm install && npm run build && git add . && node -e \"require('child_process').execSync(`git commit -m v${require('./package.json').version}`, { stdio: 'inherit' })\"",
    "postpublish": "git push"
  },
  "keywords": [
    "react",
    "slice",
    "state",
    "tuple"
  ],
  "author": "Bryan Field <bfield@700software.com>",
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@typescript-eslint/eslint-plugin": "^7.15.0",
    "eslint-config-next": "^14.2.4",
    "react": "^18.3.1",
    "typescript": "^5.5.3"
  }
  "license": "Unlicense"
}
